<%= form_for(@suggestion) do |f| %>
  <% if @suggestion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@suggestion.errors.count, "error") %> prohibited this suggestion from being saved:</h2>

      <ul>
      <% @suggestion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br>
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :suggestion_texto %><br>
    <%= f.text_field :suggestion_texto %>
  </div>
  <div class="field">
    <%= f.label :suggestion_fecha %><br>
    <%= f.date_select :suggestion_fecha %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
