<%= form_for(@has_vehicle) do |f| %>
  <% if @has_vehicle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@has_vehicle.errors.count, "error") %> prohibited this has_vehicle from being saved:</h2>

      <ul>
      <% @has_vehicle.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :vehiculo %><br>
    <%= f.text_field :vehiculo %>
  </div>
  <div class="field">
    <%= f.label :user %><br>
    <%= f.text_field :user %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
